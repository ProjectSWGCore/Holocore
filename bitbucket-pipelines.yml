image: openjdk:10-jdk-slim

pipelines:
  default:
    - step:
        caches:
          - gradle
        image: openjdk:10-jdk-slim
        script:
          - apt-get update
          - apt-get install -y git
          - git submodule update --init
          - ./gradlew shadowJar
